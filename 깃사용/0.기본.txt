기본 원리

로컬(내컴퓨터) / 리모트(깃 홈페이지 서버)

로컬은 3부분
working dir / index(stage) / head

working dir에서 add로 index(stage)에 올림

commit을 통해 head로 확정

head에서 리모트서버 갈 준비를 함

push를 통해 원격->리모트로 갱신

pull은 리모트->원격으로 갱신

branch로 현재 상태를 복사 분리 가능 - 새로운 일부 기능 추가시 유용